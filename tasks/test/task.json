{
    "id": "70d84ddb-3d01-4643-83b0-534e63c85a30",
    "name": "FlutterTest",
    "friendlyName": "Flutter Test (Extended)",
    "description": "Executes all tests for a Flutter project.",
    "helpMarkdown": "For more information, take a look at the Flutter [documentation](https://flutter.io)",
    "category": "Test",
    "author": "Aloïs Deniel, extended by Bas Rops",
    "version": {
        "Major": 0,
        "Minor": 2,
        "Patch": 55
    },
    "instanceNameFormat": "Flutter Test (Extended)",
    "inputs": [
        {
            "name": "projectDirectory",
            "type": "filePath",
            "label": "Path to the project folder",
            "defaultValue": "",
            "helpMarkDown": "The path to the project folder (which contains the `pubspec.yaml`)",
            "required": true
        },
        {
            "name": "testName",
            "type": "string",
            "label": "Test name",
            "defaultValue": "",
            "required": false,
            "helpMarkDown": "A regular expression matching substrings of the names of tests to run."
        },
        {
            "name": "testPlainName",
            "type": "string",
            "label": "Test plain name",
            "defaultValue": "",
            "required": false,
            "helpMarkDown": "A plain-text substring of the names of tests to run."
        },
        {
            "name": "updateGoldens",
            "type": "boolean",
            "label": "Update goldens",
            "defaultValue": false,
            "required": false,
            "helpMarkDown": "Whether matchesGoldenFile() calls within your test methods should update the golden files rather than test for an existing match."
        },
{
            "name": "concurrency",
            "type": "string",
            "label": "Concurrency",
            "defaultValue": "1",
            "required": false,
            "helpMarkDown": "The number of concurrent test processes to run. (defaults to '1'). Setting this to value > 1 might make the test report inaccurate (successful tests missing, failures will probably still show up fine), but the task will take less time to complete."
        },		
        {
            "name": "coverage",
            "type": "boolean",
            "label": "Coverage",
            "defaultValue": false,
            "required": false,
            "helpMarkDown": "Whether to collect code coverage information."
        },
        {
            "name": "verbose",
            "type": "boolean",
            "label": "Verbose",
            "defaultValue": false,
            "required": false,
            "helpMarkDown": "Whether to log verbose information."
        }
    ],
    "execution": {
        "Node": {
            "target": "index.js"
        }
    }
}